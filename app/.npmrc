runtime = electron
disturl = https://electronjs.org/headers
target = 40.1.0
; Copy file: dependencies into node_modules instead of symlinking them, so that
; their transitive dependencies are resolvable when install lifecycle scripts run.
install-links = true
